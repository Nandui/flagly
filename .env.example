# Postgres connection string (Neon on Vercel in production)
DATABASE_URL="postgresql://user:password@localhost:5432/flagly?schema=public"

# next-auth v5
AUTH_SECRET="generate-with: openssl rand -base64 32"
AUTH_TRUST_HOST="true"
NEXTAUTH_URL="http://localhost:3000"
